#There's a reduced form of range() - range(max_value), in which case min_value is implicitly set to zero:

for i in range(2 ** 2): #** means power two, so it counts for range(0, 4) that is 0, 1, 2 ,3
    print("Hello World")
