#If a list consists of numbers, you have to use the generators. To print out the elements of a list, separated by spaces:

a  = [1, 2, 3]

print(' '.join([str(i) for i in a])) # declaring elem as string , to make join method work on it


